<script type="text/javascript">
$(document).ready(function() {

// If it's paid for give color indication
$('.paid').each(function() {
  if ($(this).text() == 'Yes') {
    $(this).addClass('success-cell');
    $(this).css('color', 'white');
  } else {
    // Will make unpaid items have red cell
    // $(this).addClass('danger-cell');
    // $(this).css('color', 'white');
  }
  });

}); // end ready
</script>

<div class="container">
  <div class="row">
    <div class="span12 custom-block">

      <% if notice %>
      <div class="alert alert-success fade in">
        <a class="close" data-dismiss="alert" href="#">&times;</a>
        <%= notice %>
      </div>
      <% end %>

<<<<<<< HEAD
      <% if alert %>
      <div class="alert alert-success fade in">
        <a class="close" data-dismiss="alert" href="#">&times;</a>
        <%= alert %>
      </div>
      <% end %>

      <div id="purchases-table" class="margin-five">

        <h2>Ordered Items</h2>

        <table class="table table-bordered">
          <tr>
            <th class="hidden-phone"></th>
            <th>Product</th>
            <th>Qty</th>
            <th>Full Price</th>
            <th>Paid For</th>
          </tr>
          <% for purchase in @purchases %>
          <td class="hidden-phone">
=======
<% if alert %>
<div class="alert alert-success fade in">
    <a class="close" data-dismiss="alert" href="#">&times;</a>
<%= alert %>
</div>
<% end %>
<div style="margin-left: 40px; margin-right: 40px">
<h2>Ordered Items</h2>
<% for order in @orders %>
<h3><%= order == "unpaid" ? order.titleize : "Order Id:" + order %></h3>
<table class="table table-hover">
  <tr>
    <th></th>
    <th>Product</th>
    <th>Qty</th>
    <th>Full Price</th>
    <th>Paid For</th>
    <th>Purchase Time</th>
  </tr>
  
  <% purchases = Purchase.where("user_id = ? AND order_set = ?", current_user.id, order) %>
  <% for purchase in purchases %>
  <td class="hidden-phone">
>>>>>>> 561e108618cd26cc095b1c350dfa4ecd02d6214c
            <% if !(purchase.item.item_photo_file_name.nil?) %>
            <media>
              <%= image_tag(purchase.item.item_photo.url(:thumb)) %>
            </media>
            <%end%>
<<<<<<< HEAD
          </td>
          <td><%= purchase.item.name %></td>
          <td><%= purchase.quantity %></td>
          <td><%= number_to_currency(purchase.quantity*purchase.item.price) %></td>
          <td class="paid"><%= purchase.paid ? "Yes" : "No" %></td>
        </tr>
      </tr>
      <% end %>
    </table>


    <!-- Divider -->
    <ul class="nav nav-list">
      <li class="divider"></li>
    </ul>


    <h4 style="text-align: right;">Total: <%= number_to_currency current_user.pending_payment %></h4>
  </div>

</div>
</div>
=======
  </td>
  <td class="hidden-phone item_row"><%= purchase.item.name %></td>
  <td class="hidden-phone item_row"><%= purchase.quantity %></td>
  <td class="hidden-phone item_row"><%= number_to_currency(purchase.quantity*purchase.item.price) %></td>
  <td class="hidden-phone item_row"><%= purchase.paid ? "Yes" : "No" %></td>
  <td class="hidden-phone item_row"><%= purchase.created_at.in_time_zone("Central Time (US & Canada)")  %></td>
</tr>
</tr>
<% end %>
</table>
<% end %>
<h5 id="cart_total" style="float:right; margin-right: 10px">Total: <%= number_to_currency current_user.pending_payment %></h5>
>>>>>>> 561e108618cd26cc095b1c350dfa4ecd02d6214c
</div>